import { Row, Col } from 'react-bootstrap';

const ErrorPage = () => (
  <Row className="">
    <Col className="text-center">
      <div>
        <h2>We could not find any results to match your search...</h2>
      </div>
    </Col>
  </Row>
);
export default ErrorPage;
